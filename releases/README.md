# Release Records

This folder stores release-facing source-controlled records:

- release notes;
- known limitations;
- package manifests;
- checksum manifests where appropriate.

Full generated release packages must not be committed here. They are created in local `dist/` and transferred to the approved controlled release location.
