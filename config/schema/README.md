# Runtime Schemas

This folder contains machine-readable contracts for the runtime configuration and optional execution-result JSON.

Planned files:

- `eMAS_Runtime_Config.schema.json`
- `eMAS_Execution_Result.schema.json`
- `eMAS_Schema_Compatibility.json`

The engine must reject incompatible or invalid mandatory runtime configuration with a clear error.
